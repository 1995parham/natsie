apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "natsie-chart.configMapName" . }}
  labels:
    {{- include "natsie-chart.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- include "natsie-chart.configYaml" . | nindent 4 }}
